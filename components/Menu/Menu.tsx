export const Menu = ({ onClick }: any) => {
  return (
    <button
      onClick={onClick}
      className="text-gray-500 hover:text-gray-700 focus:outline-none"
    >
      Menu
    </button>
  );
};
